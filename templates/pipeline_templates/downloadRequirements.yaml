steps:
  - task: DownloadPipelineArtifact@2
    displayName: Download environment config artifact
    inputs:
      artifactName: EnvConfig
      targetPath: "$(System.DefaultWorkingDirectory)/<<.sharedInput.directoryPath>>"
